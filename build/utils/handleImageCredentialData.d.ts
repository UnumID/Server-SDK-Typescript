import { CredentialData } from '@unumid/types';
/**
 * Helper to handle image credential data.
 * Note: the credential schema by credential type should have been confirmed by this point.
 * @param data
 * @returns
 * @throws CustError
 */
export declare function handleImageCredentialData(data: CredentialData): Promise<CredentialData>;
//# sourceMappingURL=handleImageCredentialData.d.ts.map